# 📦 Smart-Photo Changelog

## [1.0.72] - 2026-09-14

### Sync API High-Performance Compression & Collection Catalog Delivery
- **Gzip-компрессия на порту синхронизации (`internal/syncapi`)**:
  - Внедрен `gzipMiddleware` для сервиса синхронизации (порт 18089). Ответы эндпоинта `/api/v1/albums` сжаты с 2.1 МБ до ~120 КБ (в 18 раз!), а `/api/v1/folders` — с 230 КБ до ~20 КБ.
  - Полностью устранены обрывы соединений (`SSLHandshakeException: connection closed`) при прохождении через облачные KeenDNS-туннели (`netcraze.link`).
- **Кэширование дерева папок (`cachedFolderTree`)**:
  - Иерархическое дерево папок теперь строится один раз во время фоновой индексации и кэшируется в оперативной памяти роутера.
  - Запрос `/api/v1/folders` отдается за 0.001 мс без повторного обхода 85 000 фото и без нагрузки на CPU роутера.
- **Единый сжатый каталог коллекций (`/api/v1/sync/catalog` и `CompactDatabase`)**:
  - Структура `CompactDatabase` расширена полями `Albums`, `FolderTree`, `Trips`, `Places` и `Categories`.
  - Зарегистрирован быстрый эндпоинт `/api/v1/sync/catalog`, отдающий все альбомы, папки, путешествия, места и категории в одном легком Gzip-пакете (<40 КБ) за 100 мс.

## [1.0.71] - 2026-09-14

### Desktop Environment Adaptation: Windows 10/11 Default Apps & Linux MIME Visibility Filtering
- **Адаптивная видимость десктопных разделов настроек**:
  - Разделы «Системный просмотровщик фото/видео» и «Связка Desktop и роутера Keenetic» теперь отображаются **исключительно** в окружениях, поддерживающих десктопную интеграцию: **Windows 10/11 Default Apps** и **Linux Desktop MIME** (`.desktop` / `xdg-mime`), а также в приложении `SmartPhoto Desktop`.
  - На роутерах Keenetic, OpenWrt и серверах без графического интерфейса десктопные разделы автоматически скрываются, сохраняя панель управления роутером чистой и ориентированной на сетевые задачи.
  - Реализован метод `desktop.IsViewerSupported(isDesktop)` и `system.IsKeenetic()`, определяющие наличие XDG/MIME окружения, графической сессии и типа платформы.
  - Эндпоинт `GET /api/system/associations` теперь возвращает флаги `is_supported`, `system_name`, `is_desktop` и `is_keenetic`.
- **Контекстные рекомендации FFmpeg и KeenDNS**:
  - Блок с разъяснением преимуществ FFmpeg-mini («Потребление RAM в 10 раз меньше...») активируется только на роутерах Keenetic.
  - Для Desktop систем отображается специальная подсказка с рекомендациями по использованию встроенного FFmpeg-mini или системного FFmpeg с аппаратным ускорением GPU.
  - Элементы KeenDNS (проверка статуса, публикация прокси) активны только на поддерживаемых роутерах Keenetic.
- **Интеграция SmartPhoto Desktop во вкладку «Клиенты»**:
  - Во вкладку «Клиенты и интеграция со SmartPhoto» добавлена официальная карточка настольного приложения SmartPhoto Desktop для Windows, Linux и macOS с описанием технологии PowerBoost, системного просмотрщика и прямой работы с NAS.

## [1.0.70] - 2026-09-14

### Desktop Enhancements: FFmpeg Installation, Max Workers, Router Integration & System Media Viewer
- **Установка FFmpeg-mini и полного FFmpeg на Desktop (Windows, Linux, macOS)**:
  - Добавлена поддержка установки как легковесного `FFmpeg-mini`, так и полного пакета `FFmpeg` в десктопном режиме.
  - Расширены пути поиска и автодетекции: директория исполняемого файла, подкаталог `bin/`, `%APPDATA%\SmartPhoto\bin`, `%LOCALAPPDATA%\Microsoft\WinGet\Links`, пути scoop/choco.
  - Установка в 1 клик через веб-интерфейс: для `mini` — автокопирование из сборки дистрибутива или компиляция из `cmd/ffmpeg-mini`; для `system` — автоматическая установка через Windows Package Manager (`winget`), `apt-get` или `brew`.
  - Сборщик `build_desktop.bat` теперь автоматически компилирует `ffmpeg-mini` для всех целевых платформ рядом с приложением.
- **Потоки фоновой обработки по умолчанию на максимум (`runtime.NumCPU()`, до 32 потоков)**:
  - Верхний лимит параллельных воркеров расширен с 8 до 32 потоков.
  - При запуске SmartPhoto Desktop автоматически выставляет `MaxConcurrentWorkers = runtime.NumCPU()`, полностью утилизируя многоядерные процессоры ПК для быстрой генерации превью и AI.
  - В настройках веб-интерфейса селектор потоков расширен вариантами 8, 16, 24, 32 потока.
- **Связка Desktop и роутера Keenetic (UI, API проверки и пошаговые инструкции)**:
  - Добавлена карточка управления связкой в настройках с селектором режимов (Прямой доступ через SMB/сетевой диск `\\172.16.1.1\photos`, Smart-Sync REST API, Автономный режим).
  - Эндпоинт `POST /api/desktop/router/test`: проверка доступности роутера, пинга (мс), валидации токена авторизации и отображение количества фото в медиатеке роутера.
  - Подробное иллюстрированное руководство, объясняющее преимущество прямого доступа через общую папку роутера (Desktop берет на себя тяжелую нагрузку на 32 потоках, а роутер отдает готовое с 0% CPU).
- **Регистрация в системе как просмотровщик фото и видео по умолчанию**:
  - Полная поддержка Windows 10/11 Default Apps framework: ветка `Capabilities\FileAssociations`, регистрация в `RegisteredApplications` и `Classes\Applications\smart-photo-desktop.exe`.
  - Регистрация всех популярных форматов фото (`.jpg`, `.jpeg`, `.png`, `.webp`, `.heic`, `.heif`, `.bmp`, `.gif`, `.tif`) и видео (`.mp4`, `.mov`, `.mkv`, `.avi`, `.webm`, `.3gp`).
  - Мгновенное обновление оболочки Windows через нативный вызов `SHChangeNotify(SHCNE_ASSOCCHANGED)`.
  - Поддержка ассоциаций Linux FreeDesktop (`.desktop` и `xdg-mime`).
  - Быстрое управление из контекстного меню системного трея («Сделать системным просмотровщиком») и из карточки в веб-интерфейсе.
- **Пиксель-перфект иконка в трее Windows, 100% идентичная веб-интерфейсу и favicon**:
  - Математически точная векторная геометрия из `favicon.svg` (диагональный градиент `#1f6feb -> #38d39f`, сквиркл `rx=5.5`, камера Lucide).
  - Многослойный ICO с набором разрешений `[16, 20, 24, 32, 48, 64, 256]` px.
  - DPI-aware выбор системного размера (`SM_CXSMICON`) и прямое создание дескриптора HICON в оперативной памяти через `CreateIconFromResourceEx` из сглаженного PNG нужного масштаба, полностью исключающее артефакты интерполяции и размытия.

## [1.0.69] - 2026-09-14

### Автоматическая раскладка по шаблону (Auto-Organizer) & Защита от дубликатов синхронизации
- **Сортировка и раскладка медиафайлов по шаблону даты (Auto-Organizer)**:
  - Возможность автоматического распределения файлов из папок загрузки (например, `Mobile`) по настраиваемым шаблонам (например, `"yyyy год/yyyy-MM-dd"`).
  - Поддержка множества источников («Может быть много Откуда»): гибкая настройка неограниченного числа независимых правил раскладки с собственными исходными и целевыми папками.
  - Поддержка богатого набора токенов форматирования: `yyyy`, `yy`, `MMMM` (полный месяц по-русски, напр. «Сентябрь»), `MMM` («сен»), `MM` («09»), `dd`, `d`, а также произвольного текста и разделителей.
  - Поддержка действий `move` (перемещение с безопасной очисткой пустых папок) и `copy` (копирование).
  - Атомарный кросс-файловый перенос `AtomicMoveFile`: безопасная обработка границ разделов и смонтированных USB-дисков (`EXDEV`) с записью во временный файл и верификацией перед удалением источника.
  - Разрешение конфликтов имён: автоматическое присвоение суффиксов (`photo_1.jpg`) при несовпадении содержимого с сохранением оригиналов.
  - **Выключено по умолчанию** (`enable_auto_organize: false`): строгое соблюдение требований безопасности роутера и сохранности данных пользователя.
- **Универсальная защита от повторной загрузки дубликатов (Universal Deduplication)**:
  - Устранение циклов «перемещение и повторная синхронизация»: Smart-Sync API сверяет файлы по всей коллекции (`FindExistingPhoto`, `FindDuplicateByDiskFile`). Если файл был перемещен в другую папку или архив, синхронизация распознает его как `UpToDate`, предотвращая повторные загрузки.
  - Мгновенная $O(1)$ фильтрация кандидатов в RAM по точному размеру файла в байтах с последующей быстрой сверкой заголовков первых/последних 4KB без нагрузки на CPU и диск роутера.
- **Интерактивный Web UI & REST API в Настройках**:
  - Новый раздел в «Настройках»: «📁 Автоматическая сортировка и раскладка по шаблону (Auto-Organizer)».
  - Удобный редактор правил с быстрым выбором готовых шаблонов дат («чипы» пресетов), выбором действий, триггеров и интеграцией с диалогом обзора папок роутера (`/api/system/browse`).
  - Режим безопасного предпросмотра (Dry-Run): детальный отчет о найденных файлах, путях назначения, статусах («К перемещению», «Дубликат в коллекции», «Конфликт имени»).
  - Запуск раскладки на лету с подтверждением, моментальным уведомлением и авто-переиндексацией галереи.
  - Новые REST API эндпоинты: `GET/POST /api/organize/rules`, `POST/DELETE /api/organize/rule`, `POST /api/organize/preview`, `POST /api/organize/run`.

## [1.0.68] - 2026-09-14

### Full-Fledged Windows Desktop GUI, Native System Tray & Embedded Application Resources
- **Полноценное GUI-приложение без окна консоли и сервера**:
  - Бинарный файл `smart-photo-desktop.exe` компилируется с флагом подсистемы `-H windowsgui`, полностью исключая появление черного окна терминала при запуске.
  - В бинарный файл встроены официальные Windows-ресурсы (`.syso`): фирменная иконка Smart-Photo высокого разрешения (256x256), манифест Windows GUI (поддержка масштабирования DPI, темы и визуальные стили Windows 10/11), название и метаданные продукта.
  - В проводнике Windows (Explorer), панели задач и при переключении окон по Alt-Tab отображается нативная иконка приложения вместо стандартного значка.
- **Запуск в виде автономного окна приложения (App Mode)**:
  - Вместо открытия ссылки во вкладке общего браузера приложение запускается в отдельном нативном окне приложения без адресной строки, навигации и закладок (`--app=...`) с изолированным профилем данных (`AppProfile`).
- **Нативный системный трей Windows (Pure Go Win32 API, `CGO_ENABLED=0`)**:
  - Цикл обработки сообщений Win32 привязан к потоку операционной системы (`runtime.LockOSThread()`), гарантируя надежную доставку системных событий.
  - Загрузка иконки высокого разрешения напрямую из встроенных ресурсов PE-модуля.
  - Контекстное меню по правому клику: Открыть Smart-Photo, статус роутера, принудительная синхронизация, пауза/возобновление фонового анализа, безопасный выход.
  - Корректное скрытие контекстного меню при клике в любое место экрана (`PostMessage(hWnd, WM_NULL, 0, 0)`).
  - Чистое закрытие приложения: выбор пункта «Выход» в трее корректно останавливает фоновый HTTP-сервер, сохраняет кэш индекса на диск и сразу удаляет значок из системного трея (`NIM_DELETE`).
- **Улучшение процесса сборки (`build.bat`)**:
  - В интерактивное меню `build.bat` добавлен пункт `[9] Build Windows Desktop GUI Client [smart-photo-desktop.exe]`.

## [1.0.67] - 2026-09-14

### Pure Classification Tagging on Router & Delegation of Person Recognition to Desktop
- **Отказ от попыток распознавания конкретных людей на слабом CPU роутера**:
  - Алгоритм анализа на роутере переведен в режим чистой классификации: кадры с людьми получают тег `people` (и `portrait` при портретной ориентации) без генерации фиктивных лиц и боксов.
  - Устранено слепое автокластерирование (`autoClusterPersonsLocked`), разбивавшее случайные фото на пустые карточки `Персона 1..22`.
  - Из хранилища `people.json` автоматически вычищены все фиктивные карточки `Персона X`, созданные прежним эвристическим алгоритмом роутера.
- **Эксклюзивное распознавание персон в Smart-Photo Desktop**:
  - Полноценное распознавание лиц, сопоставление эмбеддингов и именование персон закреплено исключительно за настольным приложением **Smart-Photo Desktop** с синхронизацией результатов на роутер через API (`POST /api/ai/faces`).
  - В веб-интерфейсе в разделе «Люди» добавлена прямая кнопка быстрого перехода «👥 Показать все фото с людьми», фильтрующая медиатеку по тегу `people`.

## [1.0.66] - 2026-09-14

### Multi-Day Vacation Clustering & Cross-Directory Trip Consolidation
- **Единая кластеризация многодневных отпусков и путешествий без GPS**:
  - Устранено дробление многодневных поездок на множество разрозненных 1-дневных мини-путешествий (например, «Мини-путешествие в Анталью» 62, 175, 92, 163 фото объединено в единое полноценное «✈️ Путешествие в Анталью 2013 Сентябрь» на 493 фото и 5 дней).
  - Хронологическая кластеризация по временным интервалам: фото из смежных папок (например, `2013-09-13 Турция`, `2013-09-14 Турция`, `2013-09-15 Турция`, `2013-09-16 Турция`, `2013-09-17 Турция`) с разрывом между кадрами $\le 96$ часов (4 дня) объединяются в единое путешествие.
  - Поддержка одиночных завершающих кадров (день выезда с 1-2 фото больше не теряется, а включается в общий отпуск).
  - Для поездок по России: объединение одного города с интервалом $\le 96$ч, и автопутешествий/туров между разными городами России с интервалом $\le 48$ч.
  - Автоматическое объединение с перекрывающимися GPS-поездками (`overlap deduplication`).
  - Фильтрация аномальных выбросов времени (сбои часов камеры, например 2001 год в папке 2007 года) с защитой от многолетних ложных интервалов.
- **Автономное определение путешествий по папкам**:
  - Анализ путешествий по каталогам (Pass 6) теперь выполняется безусловно, даже если в медиатеке нет GPS-координат или домашний город пользователя еще не определен.

## [1.0.65] - 2026-09-14

### Native Windows System Tray, Standalone App Window & Pure GUI Mode
- **Полноценное GUI-приложение без окна консоли**:
  - Бинарный файл `smart-photo-desktop.exe` переведен на подсистему `windowsgui` (`-ldflags "-H windowsgui"`), благодаря чему при запуске больше не появляется черное окно терминала/сервера.
- **Запуск в виде автономного окна приложения (App Mode)**:
  - Вместо открытия новой вкладки в общем браузере Smart-Photo Desktop запускается как полноценное отдельное нативное окно приложения (без адресной строки, навигационных панелей, закладок и лишних вкладок) с собственным изолированным профилем (`AppProfile`).
- **Нативный значок в системном трее Windows**:
  - Реализован системный трей на базе Win32 API (`Shell_NotifyIconW`), работающий на чистом Go (`CGO_ENABLED=0`).
  - Поддержка всплывающего контекстного меню по правому клику мыши (Открыть интерфейс, Статус роутера, Синхронизация, Пауза/Возобновление AI, Выход).
  - Разворачивание/фокусировка окна приложения по одиночному или двойному клику левой кнопкой мыши по значку в трее.
  - Динамическое обновление подсказки при наведении курсора (статус роутера, объем медиатеки).
- **Официальная иконка приложения**:
  - Добавлен генератор иконки SmartPhoto в формате Windows ICO (`internal/desktop/icon.go`).

### Explore Trip Filtering, Active State & Instant Photo View Fix
- **Мгновенный просмотр фотографий путешествий по клику на чип (Explore Trips & Quick Filters)**:
  - Устранена проблема отсутствия фотографий при клике на чип поездки («✈️ Мини-путешествие в Санкт-Петербург 2004 Август 57»):
    - Функция `filterByTrip` теперь автоматически очищает все конфликтующие параметры поиска (остаточные фильтры по году, строке поиска, камере, персонам), гарантируя выдачу всех 100% фотографий поездки.
    - Добавлен плавный автоматический скролл экрана (`scrollIntoView`) к блоку результатов, исключая иллюзию пустого экрана при скрытом за пределами видимости блоке результатов.
    - Лимит возвращаемых фото в режиме поездок расширен до 1000, обеспечивая загрузку полного фотоальбома поездки.
  - **Индикация активного состояния и переключение фильтра**:
    - Добавлена визуальная подсветка активного чипа (`.trip-chip.active`, `.place-chip.active`, `.person-chip-card.active`, `.category-chip.active`) с неоновым свечением и рамкой.
    - Повторный клик по активному чипу теперь мягко сбрасывает фильтр (toggle off).
  - **Информативная панель результатов и прямое открытие как альбома**:
    - В шапке результатов отображается заголовок поездки, даты проведения, количество дней и счетчик фото.
    - Добавлена кнопка «📂 Открыть как альбом», переводящая просмотр поездки в полноэкранный режим альбомов.
    - На каждом чипе поездки добавлена кнопка быстрого перехода в альбом (`📂`), позволяющая в 1 клик открыть альбом без перехода к поиску.
    - Аналогичная изоляция фильтров, авто-скролл и переключение привязаны к персонам, местам и умным категориям.
  - Добавлено экранирование идентификаторов поездок (`encodeURIComponent`) при обращении к API.

## [1.0.64] - 2026-09-14

### Timeline Upward Infinite Scroll, Clear FFmpeg-mini/System Status, Extended Destination Clustering (Yaroslavl Weekend Trips)
- **Бесшовный скролл фотоленты вверх из архива (Upward Infinite Scroll & Date Jumps)**:
  - При переходе к любой архивной дате через скруббер (например, 2022 год) реализована автоматическая подгрузка более новых фотографий (`loadPrecedingPhotos`) при прокрутке вверх.
  - Сохранение абсолютной позиции скролла при вставке элементов (`window.scrollTo({ top: prevScroll + heightDiff, behavior: 'instant' })`) без визуальных скачков экрана.
  - Добавлен адаптивный баннер архива с кнопками «▲ Загрузить выше» и «⬆️ К новым фото».
  - Добавлена плавающая кнопка быстрого возврата к самым новым фотографиям в ленте.
- **Индикация и управление FFmpeg / FFmpeg-mini**:
  - Четкое разделение и отображение активного обработчика видео: «⚡ FFmpeg-mini активен (Рекомендуется, 1.1 МБ, 1 поток, 0% CPU)», «🎬 Полный FFmpeg активен (Entware, 40 МБ libffmpeg)», «🖼️ Pure Go режим (без FFmpeg)».
  - Добавлены чипы статуса `[Установлен]` / `[Не установлен]` для каждого обработчика прямо в переключателе настроек.
  - Добавлен вывод информативного блока с текстом ошибки, если установка через opkg завершилась с ошибкой.
  - Мгновенное переключение режимов обработки видео без перезапуска сервиса.
- **Расширенная детекция путешествий по городам и объединение папок (Ярославль и др.)**:
  - Добавлен полный каталог городов (~120 городов России и мира) и динамическое подключение базы городов офлайн-геокодера.
  - Кластеризация снимков без GPS по временному интервалу (gap $\le 72$ ч) объединяет разбитые папки одного выезда (например, «2022-12-03 Ярославль» и «2022-12-04 Ярославль») в единое 2-дневное мини-путешествие: «✈️ Мини-путешествие в Ярославль 2022 Декабрь».
  - Устранена ошибка преждевременного выхода при отсутствии GPS-координат.

### Smart-Photo Desktop (GUI) Architecture & Multi-Platform Client (Win, Linux, Mac)
- **Архитектура и мультиплатформенный клиент Smart-Photo Desktop**:
  - Спроектировано и реализовано настольное приложение для Windows, Linux и macOS (`cmd/smart-photo-desktop`).
  - Поддержка трех режимов запуска: полноэкранная веб-галерея, легковесный просмотрщик фото (`--viewer <path>`), фоновый агент в системном трее (`--tray`) и пакетный процессор (`--powerboost`).
- **Бесконфликтный совместный доступ к архиву на NAS (Direct NAS Sharing & Power Boost)**:
  - Введены переносимые детерминированные идентификаторы фотографий (`root::rel_path`), обеспечивающие 100% совпадение ID и кэша превью между роутером (Keenetic) и настольным ПК.
  - Атомарная запись превью и метаданных через временные файлы и атомарный `os.Rename`.
  - Добавлена кооперативная неблокирующая аренда хранилища (`.smartphoto.lock`) с автосбросом устаревших блокировок.
  - **PowerBoost**: ПК задействует все ядра CPU (до 16 потоков) для генерации превью и нейросетевого AI-анализа с сохранением прямо в общую папку NAS; роутер подхватывает готовый результат с **0% нагрузки на CPU роутера**.
- **Полноценный системный просмотрщик фото (Viewer Mode)**:
  - Мгновенный запуск (< 200 мс) с аппаратным масштабированием, поворотом и навигацией колесом мыши/стрелками.
  - Информационная панель EXIF (камера, фокусное расстояние, диафрагма, выдержка, ISO).
  - Быстрые действия: «📁 Включить папку в медиатеку» (автоматическое добавление в `photos_dirs`), «🚀 Загрузить в роутер» (чанковая загрузка через Smart-Sync REST API) и «🖼 В галерею SmartPhoto».
- **Автообнаружение внешних накопителей (USB/SD) и структурная заливка**:
  - Монитор монтирования съемных накопителей (`USBWatcher`) для Windows, Linux и macOS.
  - Раскладка файлов по структуре «Год / Дата» (`YYYY/YYYY-MM-DD`), помесячно или по событиям.
  - Предварительная дедупликация (пропуск файлов, уже существующих в архиве или на роутере, по размеру, хэшу и EXIF).
- **Управление из системного трея (System Tray Integration)**:
  - Индикация статуса подключения к роутеру, количества фото и активности задач.
  - Меню быстрого открытия, принудительной синхронизации, паузы фонового AI-анализа и безопасного выхода.
- **Интеграция в операционные системы**:
  - Регистрация в реестре Windows (`HKCU\Software\Classes\SmartPhoto.Viewer`) и Linux (`.desktop` / `xdg-mime`).
  - Добавлены скрипты сборки `build_desktop.bat` и конфигурация Wails `desktop/wails.json`.

## [1.0.63] - 2026-09-14

### Multi-Arch ffmpeg-mini Packages, Engine Priority Selector, Mini-Trips, Smart Categories & Auto-Clustered People
- **Исправление двойного знака ✈️ ✈️ в названии путешествий**:
  - Устранен дублирующийся эмодзи самолета в таймлайне, баннерах, заголовках альбомов и чипах фильтров.
- **Расширенный поиск путешествий и мини-путешествий (1–2 дня)**:
  - Снижен порог детекции до $\ge 2$ GPS-фотографий для коротких выездов и поездок выходного дня (и 1 фото для удаленных от базового города локаций).
  - Окно поездки расширено до полных календарных суток (с 00:00 дня начала до 23:59 дня окончания), благодаря чему все фотографии этих дней (включая снимки без GPS) автоматически входят в состав путешествия.
  - Добавлен вторичный поиск по географическим папкам архива («Крым», «Египет», «Сочи», «Тула» и др.) для старых цифровых фотоаппаратов без геопозиции.
  - Поездки длительностью до 2 дней автоматически маркируются как «✈️ Мини-путешествие: ...».
- **Исправление и наполнение «Умных категорий и сцен»**:
  - Добавлена поддержка дублирующих полей `title`/`name` и `count`/`photo_count` в структуре `CategoryInfo`.
  - Добавлены нативные эмодзи-иконки для всех категорий (🏔️ Природа и горы, 👤 Портреты, 🔍 Макро, 🌙 Ночь, 🌅 Закаты и рассветы, 📄 Документы, 🏖️ Пляжи, 🐾 Животные, 🎬 Видео).
  - Исправлен рендеринг карточек категорий в Explore и альбомах.
- **Автоматическая кластеризация людей («Не найдено ещё ни одного человека»)**:
  - При наличии обнаруженных на фотографиях лиц нераспределенные лица автоматически объединяются в карточки персон («Персона 1», «Персона 2» и т.д.) с аватарами и счетчиками фотографий.
  - Поддержано переименование персон и просмотр их фотоальбомов.
- **Поддержка и установка `ffmpeg-mini` через общий репозиторий (`snakelair/Keenetic`)**:
  - Разработан автономный Pure Go инструмент `cmd/ffmpeg-mini` с нулевой зависимостью от сторонних C-библиотек (`CGO_ENABLED=0`).
  - Автоматическая кросс-компиляция и генерация пакетов `ffmpeg-mini_1.0.0_${ARCH}.ipk` для всех 5 архитектур (`aarch64`, `mipsel`, `armv7`, `x86_64`, `mips`) в `c:\Projects My\Keenetic\entware\`.
  - В веб-интерфейс «Поддержка видеофайлов и обработчик FFmpeg» добавлен селектор режимов (⚡ FFmpeg-mini в приоритете, 🎬 Полный FFmpeg, 🖼️ Pure Go), кнопки установки в 1 клик через `opkg` и подробный информационный блок с пояснением преимуществ (10x меньше RAM, 1 поток без нагрева CPU и лагов Wi-Fi, мгновенное чтение обложек .THM/MP4 covr, 1.1 МБ на флешке вместо 40–80 МБ).

## [1.0.62] - 2026-09-14

### Pure Go Embedded Video Cover Extraction (Variant 3) & ffmpeg-mini Prioritization (Variant 2)
- **Вариант 3: Прямое извлечение обложек видео в Pure Go**:
  - Поддержка файлов-спутников экшн-камер (`.THM` / `.thm`, `.jpg`): мгновенное извлечение превью GoPro, Canon, DJI, Sony за 0.1 мс при 0% CPU роутера.
  - Парсер MP4/MOV контейнеров в чистом Go: извлечение встроенных постеров из атомов `moov/udta/meta/ilst/covr` без запуска сторонних бинарников.
- **Вариант 2: Приоритет и поддержка легковесного `ffmpeg-mini`**:
  - Детекция `ffmpeg-mini` перед стандартным `ffmpeg` в `/opt/bin/` и `PATH`.
  - В случае отсутствия встроенной обложки видео запускается легковесный `ffmpeg-mini` с ограничением в 1 поток (`-threads 1`) и декодированием только ключевых кадров (`-skip_frame nokey`).
  - Добавлен сценарий сборки ультралегкого статического бинарника `scripts/build_ffmpeg_mini.sh` (~1.8 МБ).

## [1.0.61] - 2026-09-14

### Trips Date & Year Binding and High-Speed Low-CPU FFmpeg Optimizations
- **Привязка путешествий к годам и месяцам**:
  - Все названия путешествий теперь включают год и месяц проведения поездки (например, «✈️ Путешествие: Россия, Петропавловск-Камчатский 2026 Май», «✈️ Путешествие в Домбай 2021 Апрель», «✈️ Путешествие в Хургаду 2021 Сентябрь»).
  - Добавлена иконка самолета «✈️» в заголовок поездки для наглядного отображения в альбомах, фильтрах, таймлайне и карточках фото.
- **Оптимизация и облегчение работы FFmpeg**:
  - Ограничение потоков флагом `-threads 1`: исключена 100% загрузка всех ядер процессора роутера при извлечении постера видео.
  - Флаг `-skip_frame nokey`: декодируются исключительно ключевые кадры (I-frames), пропуская 95% тяжелых межкадровых вычислений (ускорение в 10–20 раз).
  - Флаги `-an` и `-sn`: полное отключение обработки аудиопотоков и субтитров.
  - Исправлен синтаксис масштабирования `scale=320:-2`.

## [1.0.60] - 2026-09-14

### Trips Grammar Enhancements, Flash Safety & Local AI Batch Optimization
- **Улучшение склонения названий путешествий**:
  - Добавлено склонение названий городов в винительный падеж («Путешествие в Хургаду», «Путешествие в Самару», «Путешествие в Москву», «Путешествие в Домбай»).
- **Оптимизация flash-памяти и защиты от износа USB/накопителя**:
  - Пакетное сохранение тегов (`SaveTags`) и детекций лиц (`SavePeople`) в фоновых AI-эвристиках с интервалом раз в 200 фотографий вместо сброса на диск на каждой отдельной фотографии.
  - Устранена избыточная нагрузка ввода/вывода при анализе метаданных.
  - Исключено синхронное создание миниатюр в фоновом AI-потоке при их отсутствии — миниатюры генерируются щадящим пулом воркеров.

## [1.0.59] - 2026-09-14

### Trips & Travels Auto-Detection, AI Status Hub & UI Enhancements
- **Функционал «Путешествия» (Trips & Travels)**:
  - Автоматическое определение базового (домашнего) города пользователя по анализу временного охвата (снимки на протяжении месяцев/лет) и частоты локаций.
  - Интеллектуальная детекция временных поездок (на срок до 2–3 недель) за пределы базового города с возвратом в базу.
  - Автоматическая генерация названий с учетом русской грамматики и предлогов:
    - 1 город: «Путешествие в Домбай», «Путешествие в Сочи», «Путешествие во Владимир».
    - Несколько городов в зарубежной стране: «Путешествие в Китай», «Путешествие в Турцию», «Путешествие на Кипр», «Путешествие во Францию».
    - Несколько городов в домашней стране: «Путешествие: Домбай, Теберда».
  - Отображение в **таймлайне**: стильный Glassmorphism-баннер с бейджем `✈️ ПУТЕШЕСТВИЕ`, датами, днями, количеством фото и кнопкой открытия поездки.
  - Отображение в **альбомах**: карточки поездок с бейджем `✈️ Поездка`, открывающиеся внутри раздела альбомов.
  - Отображение в **фильтрах и поиске**: выпадающий список поездок и кликабельные чипы путешествий.
  - Эндпоинты REST API: `GET /api/trips`, `GET /api/trips/{id}`, `POST /api/trips/refresh`.
- **Таб «Индексация» (`#tab-status`)**:
  - Добавлен блок статуса и управления «🧠 Умная классификация и AI-метаданные».
  - Информационные бейджи: персоны и лица, места, сцены и теги, путешествия, отображение базового города.
  - Кнопки быстрого запуска AI-анализа (для новых фото), принудительного полного пересчета и обновления путешествий/мест.
- **Таб «Настройки» (`#tab-settings`)**:
  - Блок «Умная классификация и AI-метаданные» поднят выше блока «Синхронизация и интеграция (Smart-Sync & WebDAV)».
- **Самодиагностика системы (System Doctor)**:
  - Исправлен стиль «серое на сером» для кнопок «🐞 Отправить баг-репорт» и «📋 Скопировать отчет для поддержки»: добавлены контрастные классы `.btn-outline-danger` и `.btn-outline-primary` с неоновыми контурами и полупрозрачным фоном.

## [1.0.58] - 2026-09-14

### Mountain Resorts Geocoding & Multi-Provider Map Links
- **Уточнение офлайн-геокодинга для горных курортов и туристических мест**:
  - В базу офлайн-геокодера добавлено более 60 ключевых горных, лыжных курортов и курортных городов:
    - **Кавказ**: Домбай, Теберда, Архыз, Романтик, Терскол (Приэльбрусье), Эльбрус, Чегет, Карачаевск, Черкесск, Ессентуки, Железноводск, Лермонтов, Георгиевск, Красная Поляна, Эсто-Садок, Роза Хутор, Лаго-Наки, Даховская, Дербент, Каспийск, Избербаш, Хасавюрт.
    - **Крым**: Алушта, Судак, Коктебель, Новый Свет, Гурзуф, Алупка, Симеиз, Форос, Балаклава, Саки, Бахчисарай.
    - **Алтай и Сибирь**: Шерегеш, Манжерок, Чемал, Белокуриха, Артыбаш (Телецкое озеро), Листвянка, Байкальск, Слюдянка, Хужир (Ольхон).
    - **Север и Карелия**: Кировск (Хибины), Апатиты, Териберка, Сортавала, Рускеала, Выборг.
    - **Золотое Кольцо и Центр**: Сергиев Посад, Суздаль, Переславль-Залесский, Ростов Великий, Углич, Муром, Коломна, Звенигород.
    - **Ближнее зарубежье**: Гудаури, Степанцминда, Боржоми, Бакуриани, Цахкадзор, Шымбулак.
  - Пороги расстояния оптимизированы: до 60 км — точное название курорта/города (фото из Домбая и его окрестных склонов/ущелий теперь однозначно определяются как «Домбай», а не как далекие равнинные города).
  - Улучшен алгоритм кластеризации: автоматическая миграция фото в более точный кластер при обновлении базы и автоматическая очистка пустых кластеров (`PhotoCount == 0`), предотвращающая захламление интерфейса.
  - Пакетное сохранение мест (`AssignPlacesForGPSPhotos`) без множественных повторных записей во flash-память роутера.
- **Ссылки на Google Карты и Яндекс Карты в Lightbox**:
  - В блок «Геолокация (GPS)» в боковой панели сведений о снимке добавлены быстрые кнопки для открытия координат в трех картографических сервисах:
    - **🗺️ OSM** (OpenStreetMap)
    - **📍 Google** (Google Maps)
    - **🧭 Яндекс** (Яндекс Карты с корректным порядком координат долгота/широта)
  - Ссылки открываются в новой вкладке с безопасными атрибутами `target="_blank"` и `rel="noopener noreferrer"`.

## [1.0.57] - 2026-09-14

### In-Section Album Exploration & Folder Tree Refinement
- **Просмотр фотографий внутри раздела альбомов (без перехода в общую фотоленту)**:
  - При клике на карточку альбома в сетке просмотр открывается непосредственно внутри вкладки «Альбомы» (`#tab-albums`).
  - Добавлена шапка альбома с кнопкой «Назад к альбомам», названием, счетчиком снимков, размером и путем на диске.
  - Полноэкранный просмотрщик (Lightbox) работает в контексте текущего альбома, а при закрытии пользователь остается внутри альбома.
  - Сохранена возможность перейти в общую фотоленту с фильтром по папке через кнопку «Открыть в общей фотоленте».
- **Оптимизация древовидного проводника (Tree View)**:
  - Исключен показ значка фото (`📷`) в структуре дерева: папки отображаются папками (`📁`), а промежуточные узлы раскрываются без ложного впечатления вывода отдельных фото в дереве.
  - Клик по промежуточным папкам (содержащим подпапки) сворачивает/разворачивает ветки дерева без выгрузки всех вложенных фотографий в превью.
  - Просмотр фото активируется строго при клике на **конечную папку** (leaf folder), исключая хаотичную загрузку тысяч фото из всех поддиректорий.

## [1.0.56] - 2026-09-12

### Smart Places & Document Recognition Refinement
- **Определение городов и расширенный офлайн геокодинг**:
  - Исправлено отображение городов в веб-интерфейсе: устранен баг, при котором в блоке «Места» выводилась только страна (например, `Россия` вместо `Москва, Россия`).
  - Исправлена критическая проблема сериализации `GetPhotoDetails`, из-за которой при встраивании структуры `Photo` с собственным `MarshalJSON` поля `Faces`, `Tags` и `Place` не попадали в итоговый JSON-ответ.
  - База встроенных городов офлайн-геокодера расширена более чем в 3 раза (120+ ключевых городов, региональных центров и курортов РФ и мира).
  - Введен адаптивный радиус определения: точный поиск до 75 км, интеллектуальный фоллбэк («Окрестности <Города>» до 160 км, «Регион <Города>» до 350 км), обеспечивающий покрытие для любых GPS-снимков.
  - Автоматическая мгновенная привязка мест для всех фото с GPS (`AssignPlacesForGPSPhotos`) при загрузке индекса с нулевой нагрузкой на CPU.
- **Интеллектуальное распознавание документов и чеков**:
  - Переработан алгоритм эвристического детектора документов `heuristics.go`: устранены сбои при комнатном теплом освещении смартфона благодаря адаптивной фильтрации цветовой насыщенности ($|R-G| < 26$, $S < 0.28$) вместо строгого серого оттенка.
  - Добавлен бимодальный анализ контраста документа: проверка достаточной площади светлого фона бумаги ($> 35\%$) и темного текста ($> 4\%$).
  - Добавлено контекстное распознавание по ключевым словам в имени файла и директории (`doc`, `scan`, `чек`, `квитанц`, `документ`, `bill`, `invoice`, `receipt`, `passport`, `contract` и др.).
  - Документы теперь классифицируются с наивысшим приоритетом до пейзажей (предотвращая ложную классификацию белого листа бумаги как неба).
- **Управление AI-сканированием в Web UI**:
  - В блок «Умные категории и сцены» добавлена кнопка «🧠 Анализировать фото», запускающая принудительное фоновое сканирование (`POST /api/ai/scan?force=true`).

## [1.0.55] - 2026-09-12

### AI Metadata & Scene Classification (Option 2 & Option 3 Companion API)
- **Локальный анализ сцен и лиц на роутере (Вариант 2 — Pure Go, по умолчанию отключен)**:
  - Реализован ультралегковесный анализатор `AnalyzePhotoHeuristics` на чистом Go без CGO и внешних библиотек.
  - Анализ выполняется исключительно по готовым миниатюрам 150px в фоне с адаптивными паузами, не нагружая CPU и не занимая оперативную память роутера.
  - Цветовая классификация в пространстве YCbCr (ITU-R BT.601) для определения лиц/людей.
  - Детекция сцен по цветовым гистограммам и EXIF: Пейзаж (зелень/небо), Закат (теплый горизонт), Ночь (низкая освещенность), Макро (малая глубина резкости), Документы (высококонтрастный ч/б).
  - Настройки `enable_local_ai`, `local_ai_detect_faces`, `local_ai_detect_scenes` отключены по умолчанию для обеспечения 0% нагрузки.
- **REST API для внешнего Desktop AI (Вариант 3 — Companion Worker)**:
  - `GET /api/ai/queue`: очередь неклассифицированных фото для обработки внешним ПК со скриптом/нейросетью.
  - `POST /api/ai/faces`: пакетная загрузка результатов распознавания лиц и привязки к персонам.
  - `POST /api/ai/tags`: пакетная загрузка тегов сцен и объектов.
  - Управление персонами: `GET /api/people`, `GET /api/people/{id}`, `POST /api/people/{id}/rename`, `POST /api/people/merge`.
  - Управление местами и категориями: `GET /api/places`, `GET /api/categories`.
- **Разреженное сайдкар-хранилище AI-метаданных (`.smartphoto/`)**:
  - `people.json`, `tags.json`, `places.json` сохраняются на внешнем диске.
  - Базовая структура `Photo` не увеличивается ни на один байт при отсутствии AI-тегов (0 байт лишней RAM на 85 000 фото).
  - Быстрые инвертированные индексы в памяти для мгновенной фильтрации.
  - Встроенный автономный Reverse-geocoding по координатам GPS без внешних запросов к сети.
- **Интеграция с веб-интерфейсом**:
  - Вкладка «Обзор»: витрины Людей с аватарами и счетчиками, Мест и Городов, Умных категорий и сцен.
  - Быстрая фильтрация по клику на человека, город или категорию.
  - Отображение AI-классификации в боковой панели EXIF полноэкранного просмотрщика.
  - Переименование персон прямо из веб-интерфейса.

## [1.0.54] - 2026-09-12

### Deep RAM Optimization Phase 2: Dynamic Path Slicing, Compact DB Streaming, Heap De-duplication & Zero-Copy Timelines
- **Динамический срез имени файла (`Photo.FileName()`)**:
  - Поле `FileName string` полностью удалено из структуры `Photo`, экономя 16 байт заголовка строки + длину имени файла в куче на каждый объект каталога (~3.4 МБ постоянной RAM при 85 000 фото).
  - Метод `FileName()` вычисляет имя файла на лету через срез существующей строки `RelPath` без единой аллокации в куче.
  - Сохранена 100% обратная совместимость с API и Web UI через динамический `Photo.MarshalJSON()` / `Photo.UnmarshalJSON()`.
- **Исключение уникальных идентификаторов из `internMap`**:
  - Уникальный хэш `Photo.ID` исключен из глобальной карты интернирования строк: так как ID гарантированно уникален для каждого файла, сохранение 86 000 уникальных ключей в мапе приводило к паразитному расходу ~7–8 МБ RAM при нулевом проценте повторений.
  - Размер `internMap` ограничен лимитом в 8192 записей с фильтром по длине строки (<= 256 символов) для надежной защиты от утечек памяти.
- **Потоковая генерация и отдача `compact_db.json.gz`**:
  - Полностью исключено хранение сжатого буфера `compactGzCache []byte` в оперативной памяти (экономия до 5 МБ постоянной RAM).
  - Сжатый индекс `compact_db.json.gz` стримится напрямую в файл на внешний диск и отдается клиентам через `http.ServeContent` с использованием эффективного файлового кэша ядра Linux (Page Cache).
- **Zero-Copy таймлайн (`TimelineDayGroup.Photos []*Photo`)**:
  - В срезе фото таймлайна `TimelineDayGroup.Photos` заменены тяжелые структуры (120+ байт) на компактные указатели `[]*Photo`.
  - Устранено постоянное копирование сотен килобайт данных по значению при каждом HTTP-запросе таймлайна.
- **Оптимизация бакетов сводки таймлайна**:
  - Внутренние ключи группировки месяцев в `recomputeCachedSummariesLocked` переведены с форматированных строк (`"2026-08"`) на быстрый целочисленный ключ `int` (`year*100 + month`), предотвратив 85 000 строковых аллокаций во время сканирования.

## [1.0.53] - 2026-09-12

### High-Performance RAM Optimization, Streaming Index Loading, Lazy EXIF & Adaptive Memory Tuning
- **Потоковая загрузка индекса без промежуточных срезов (`StreamIndex`)**:
  - Чтение `index.json.gz` переведено на потоковый токенайзер `json.Decoder.Token()` / `dec.More()`.
  - Элементы каталога десериализуются по одному и сразу размещаются в целевых структурах `map` и срезе.
  - Полностью устранён пиковый всплеск потребления памяти (memory spike) при старте сервиса, снизив пик RAM при перезапуске на 60%.
- **Облегчение структуры `Photo` и оптимизация выравнивания памяти**:
  - Устранено мертвое строковое поле `FullPath` (путь разрешается на лету через `GetEffectiveFullPath`).
  - Устранено поле `AspectRatio float64` (рассчитывается на лету `Width / Height`), давая выигрыш в 8 байт на каждое фото.
  - Упорядочены поля структуры для минимизации байтового паддинга компилятора, булевы флаги плотно упакованы в битовую маску байта.
  - Добавлено сквозное интернирование идентификатора `ID = Intern(ID)`: ключи мап, списки и метаданные используют общий указатель на срез байт в куче.
- **Ленивый EXIF и очистка пустых метаданных**:
  - Добавлен метод `ExifData.IsEmpty()`: для изображений без метаданных (PNG, WebP, видео, JPEG без EXIF) память под структуру больше не выделяется (`nil`).
  - В `GetPhotoDetails` реализован метод `GetPhotoWithDetails`: при клике по фото подробные параметры съемки (линза, выдержка, ISO, GPS) мгновенно считываются из первых 64 КБ файла на диске, не занимая оперативную память в покое.
- **Пул буферов `sync.Pool` и оптимизация поворотов превью**:
  - Внедрён пул `sync.Pool` для `bytes.Buffer` в генераторе миниатюр, предотвращая частые реаллокации при JPEG-сжатии.
  - Оптимизирован `applyOrientation`: поворот на 180° и отражения выполняются по месту (in-place swap пикселей) без выделения второго полного растрового буфера `image.RGBA`.
- **Адаптивный аппаратный тюнинг памяти под роутеры (`GOMEMLIMIT`)**:
  - Интегрирована функция `GetRecommendedMemoryLimit()`: сервис анализирует `/proc/meminfo` и автоматически выставляет лимит кучи Go:
    - 36 МБ для роутеров со 128 МБ RAM (Keenetic City, Air, Extra);
    - 64 МБ для роутеров с 256 МБ RAM (Keenetic Speedster, Giga III);
    - 96 МБ для роутеров с 512 МБ RAM (Keenetic Ultra, Titan, Hero);
    - 160–256 МБ для серверов и ПК.
  - Значение `MemCacheSizeMB` оптимизировано по умолчанию до 2 МБ, передавая управление кэшем файлов системному Page Cache ядра Linux.

## [1.0.52] - 2026-09-11

### SmartUtils Ecosystem Redesign, Centralized Bug Report, Instant Index Reload & QR Code Client Pairing
- **Редизайн блока «Экосистема Smart» по стандарту SmartUtils**:
  - Карточка экосистемы во вкладке «О проекте / Поддержка» перерисована на всю ширину экрана в едином премиальном стиле SmartUtils.
  - Добавлена сетка проектов: Smart-Photo (:8089, текущий сервис), Smart-Utils (:8090), Smart-Route (:8088), Smart-VPN (:8091).
  - Добавлены прямые ссылки на Telegram, форум Keenetic, репозиторий OPKG и кнопка быстрого создания багрепорта.
- **Централизованная система создания багрепортов (`bugreport.js`)**:
  - Интегрирован контроллер багрепортов со SmartUtils (`BugReportManager`).
  - Фиксированный компонент `smart-photo`, переключатель «Ошибка» / «Идея», мгновенный сбор системной автодиагностики (`POST /api/doctor/bugreport`) с отображением бейджей здоровья системы, копирование отчета в буфер и создание issue на GitHub в 1 клик с предзаполненными метками `[Smart-Photo]`.
- **Мгновенный подхват `index.json.gz` при подключении или смене папки с фото**:
  - Индекс галереи `index.json.gz` хранится исключительно на подключенном внешнем диске в скрытой служебной папке `.smartphoto`, не расходуя внутреннюю Flash-память роутера.
  - Реализован метод `ReloadIndexAsync`: при обновлении конфигурации или подключении папки сервис проверяет наличие сохраненного `.smartphoto/index.json.gz` и мгновенно загружает готовую базу фотографий в память без необходимости повторного сканирования всего диска.
- **Единый внешний адрес и сопряжение по QR-коду (Способ 1)**:
  - Внешний адрес сервера и интеграция с KeenDNS вынесены в общий блок над всеми методами сопряжения.
  - Добавлен способ сопряжения по QR-коду для мобильных приложений (`SmartPhotoSync`): телефон моментально считывает QR-код камерой с внешним адресом и сгенерированным токеном доступа, без ручного ввода настроек.
  - Сохранены и гармонизированы облачный 6-значный PIN через ntfy.sh (Способ 2) и 4-значный локальный PIN (Способ 3).

## [1.0.51] - 2026-09-11

### Direct DB Upload Insertion, fsnotify Rescan Suppression, QR Codes for Mobile Apps & Compressed index.json.gz
- **Мгновенное занесение в БД при аплоаде без пересканирования папки**:
  - При завершении выгрузки файла по Smart-Sync API файл сразу же регистрируется в галерее (`IndexSingleFile`).
  - Оптимизирована процедура `IndexSingleFile`: вместо полной $O(N \log N)$ пересортировки всех 85 000 фото реализована вставка за $O(\log N)$ через бинарный поиск (`sort.Search`), что снизило время регистрации фото со 100 мс до ~2 мс.
  - Добавлен потокобезопасный трекер внутренних загрузок (`MarkUploadedFile` / `isRecentlyUploaded`). Подсистема отслеживания изменений папок `fsnotify` теперь знает о происходящих аплоадах и игнорирует события файловой системы для выгруженных файлов, полностью предотвращая запуск избыточных сканирований каталогов (`ScanDirectory`).
- **QR-коды для быстрой установки приложений на телефон**:
  - В настройках веб-интерфейса (вкладка «Настройки») добавлен блок «Мобильные приложения & Быстрая установка» с высококонтрастными QR-кодами для мгновенного наведения камеры телефона:
    1. QR-код на прямую загрузку нативного Android-приложения `SmartPhotoSync (APK)`;
    2. QR-код для моментального открытия веб-галереи на смартфоне и установки в качестве PWA-приложения на главный экран (с автоматическим использованием внешнего домена KeenDNS при его наличии);
    3. QR-код для перехода в официальный Telegram-канал проекта `t.me/SmartKeenetic`.
  - Во вкладке «Клиенты» в блок скачивания Android APK также встроен персональный QR-код для сканирования камерой.
  - Встроен автономный легковесный Pure JS генератор QR-кодов (`qrcode.min.js`), работающий на 100% оффлайн без внешних запросов к сторонним сервисам.
- **Сжатое хранение индекса базы данных `index.json.gz` (экономия 93% Flash/RAM)**:
  - Формат сохранения главного индекса библиотеки переведен на сжатый gzip-поток `index.json.gz` (уровень `gzip.BestSpeed`).
  - Размер файла на USB-накопителе уменьшен с 68.1 МБ до ~4.8 МБ (коэффициент сжатия ~14:1, экономия 63.3 МБ).
  - Устранена просадка дискового ввода-вывода (запись длится <0.5 сек вместо 8-11 сек) и критический износ ячеек Flash-памяти.
  - Полностью ликвидирована нагрузка на ядро Linux `kswapd0` от буферизации 68 МБ dirty-страниц в условиях ограниченной оперативной памяти роутера (512 МБ).
  - Обеспечена полная обратная совместимость: функция `LoadIndex` автоматически загружает `index.json.gz`, а при его отсутствии прозрачно считывает старый `index.json` и конвертирует в сжатый формат при следующем сохранении.

## [1.0.50] - 2026-09-11

### Header Favicon, Compact DB Archive for Remote Android App & CPU Optimization
- **Значок Favicon взят из шапки (Header Logo)**:
  - Файл `favicon.svg` обновлен и теперь полностью повторяет логотип сервиса из шапки: аккуратный скругленный квадрат с фирменным градиентом (`#1f6feb` -> `#38d39f`) и четким белым силуэтом фотокамеры.
- **Очистка шапки веб-интерфейса**:
  - Удалена статическая плашка с моделью роутера (`Keenetic Router`) из шапки, делая заголовок более лаконичным и чистым.
- **Специальный сверхкомпактный архив базы данных для мобильного приложения Android (`/api/v1/sync/db/compact`)**:
  - Добавлены эндпоинты `/api/v1/sync/db/compact`, `/api/v1/sync/db/compact.gz` (порт 18089) и `/api/sync/db/compact`, `/api/photos/compact-db` (порт 8089) для мгновенной удаленной синхронизации Android-приложения через KeenDNS или мобильную сеть.
  - Поддержка мгновенного определения общего числа фото через HTTP-заголовок `X-Total-Photos: <count>` (доступно даже в HEAD-запросах без чтения тела).
  - Сжатие `gzip` (Level 9): 85 000 фото архивируются всего в ~600-800 Кб вместо исходного 68 Мб `index.json`.
  - Структура архива включает готовые блоки таймлайна (`years`, `months`, `days`) с количеством и смещениями (offset) для мгновенного построения плавающего скроллбара (scrubber), идентичного веб-версии, а также сверхкомпактный массив элементов фото (`id`, `t`, `w`, `h`, `sz`, `fmt`, `fav`, `is_vid`).
  - Поддержка параметра `?summary_only=true` (возврат только метаданных скроллбара размером <10 Кб) и дискового кэширования `compact_db.json.gz` для отдачи с 0% CPU.
- **Оптимизация нагрузки на процессор и диск роутера**:
  - Устранена причина скачков CPU и блокировки дискового ввода-вывода (I/O `D` state): при каждой загрузке фото со смартфона сервис выполнял синхронную перезапись файла `index.json` размером 68 Мб на USB-диск.
  - Реализован отложенный дебаунс сохранения индекса `SchedulePersistIndex` (30-секундное окно группировки пакетных записей).
  - В `ScanDirectory` добавлено быстрое сопоставление подкаталогов по `p.Directory` и short-circuit: если на диске не обнаружено изменений, ресурсоемкая сортировка 85 000 фото, пересчет сводок и запись 68 Мб на диск полностью пропускаются.
  - Увеличен таймер дебаунса `fsnotify` с 2.5 сек до 10 сек для предотвращения шторма пересканирований при активной передаче фото со смартфонов.

## [1.0.49] - 2026-09-11

### 24h Scheduled Night Autoscan (03:00) & KeenDNS 1-Click Port Publishing
- **Суточная периодичность автосканирования по умолчанию (1440 минут)**:
  - Периодичность автоматического пересканирования библиотеки установлена по умолчанию в 1 сутки (`1440` минут) вместо 60 минут.
  - Добавлено настраиваемое начальное время сканирования `scan_start_time` (по умолчанию `03:00`), благодаря чему фоновое сканирование фотоархива проводится ночью.
  - Реализован планировщик `NextScheduledScanDelay` с привязкой к целевому слоту времени, переходом через полночь и динамическим перезапуском таймера `RestartPeriodicScan` при изменении настроек в веб-интерфейсе без перезагрузки демона.
- **Интеграция с KeenDNS для облачного 6-значного PIN-сопряжения**:
  - Добавлено наглядное разъяснение в веб-интерфейсе: для сопряжения со смартфонами и ТВ через интернет (мобильную сеть) URL сервера должен быть внешним, а не локальным IP-адресом.
  - Реализован модуль взаимодействия с KeeneticOS CLI (`internal/keenetic` через `ndmc`), автоматически определяющий зарегистрированное имя KeenDNS (`show ndns`) и опубликованные прокси-правила (`show ip http proxy`).
  - Сервер автоматически подставляет внешний URL `https://spsync.<domain>` в поле сопряжения при наличии опубликованного прокси.
  - Добавлена кнопка **«Опубликовать spsync.<domain> в KeenDNS в 1 клик»**: выполняет команды публикации прокси для порта Smart-Sync (`18089`) в режиме `security-level public` и `no auth` (без парольной защиты роутера, так как у Smart-Sync есть встроенная защита по токену).

## [1.0.48] - 2026-09-11

### High-Performance Video Thumbnail Extraction & Automatic Poster Upgrade
- **Аппаратное SIMD-масштабирование миниатюр видео через FFmpeg**:
  - Генерация миниатюр видео переведена на прямое масштабирование FFmpeg (`-vf scale=min(320,iw):-2`) вместо тяжелого декодирования 1080p/4K кадров в Go-память.
  - Потребление оперативной памяти снижено с десятков мегабайт до ~15 Кб, исключены OOM-сбои и таймауты при обработке видео.
- **Автоматическая замена заглушек видео при установке FFmpeg**:
  - Добавлено автообновление кэша: если ранее для видеофайла была сгенерирована минимальная заглушка (placeholder), то при наличии установленного FFmpeg демон автоматически генерирует и сохраняет настоящий кадр из видео.

## [1.0.47] - 2026-09-11

### KeeneticOS Storage Protection & Zero-Collision Safe Mounting
- **Защита от создания фантомных папок на несмонтированных дисках (`/tmp/mnt/`)**:
  - Реализован модуль безопасной работы с файловой системой `storage.SafeMkdirAll`: перед созданием каталогов сервис проверяет `/proc/mounts`, блокируя создание папок на несмонтированных точках.
  - Устранена коллизия с подсистемой Keenetic `ndm`: предотвращено создание структуры кэша `.smartphoto/thumbs` в оперативной памяти (`tmpfs`) во время раскрутки внешних механических HDD, что вызывало ошибку `unable to mount: filesystem with same UUID already mounted (directory not empty)`.
  - **Автоматическое ожидание монтирования внешних дисков (`WaitForStorageMount`)**: при старте роутера демон ожидает готовности USB-диска до 45 секунд без блокировки веб-сервера и системного пула сервисов.
  - **Диагностика в System Doctor**: модуль диагностики теперь сверяет пути с `/proc/mounts`, выявляет активные точки монтирования и предупреждает о наличии несмонтированных каталогов-пустышек в RAM.

### Google Photos-Exact Scrubber Overhaul & 0ms Zero-Lag Dragging
- **Пропорциональное распределение годов по количеству фото**:
  - Высота интервала между годами на скроллере теперь напрямую зависит от доли фотографий в библиотеке (`count / totalPhotos`).
  - Равномерное размещение меток-точек месяцев (3, 2, 1 или 0 точек в зависимости от плотности фото и доступного зазора) с защитой от наложения меток.
- **Устранение подсветки и масштабирования годов при ведении**:
  - Года отображаются в минималистичном дизайне (чистый текст без рамок, фонов и всплывающего масштабирования).
  - При перемещении и скроллинге элементы под курсором не перехватывают фокус и не подсвечиваются.
- **Устранение притормаживания (0ms Lag)**:
  - Удалена конфликтующая CSS-анимация `transition` во время отслеживания курсора.
  - Рендеринг позиции индикатора переведён на аппаратный `translate3d` через `requestAnimationFrame`.
  - Кэширование размеров трека при наведении/нажатии исключает принудительные перерисовки (reflow) на каждом событии мыши.
  - Исправлен переход к дате: при скроллинге ленты вниз после перехода загрузка продолжается без фильтрации.

## [1.0.46] - 2026-09-11

### Google Photos-Style Fast Scrubber & Smooth Photo Zoom
- **Интерактивный скроллер ленты в стиле Google Photos**:
  - Быстрый переход по годам и месяцам вдоль правой границы экрана с индикацией текущей даты.
  - Автоматическое скрытие навигационной панели при отсутствии движения и плавная анимация появления при прокрутке.
- **Масштабирование фото колесом мыши с центрированием на курсор**:
  - Улучшенная навигация и зум в полноэкранном просмотрщике фото.

## [1.0.45] - 2026-09-10

### Reliable Large File Uploads & Synchronization Concurrency Guard
- **Устранение обрывов выгрузки больших файлов (~250Мб+)**:
  - **Потоковое вычисление контрольных сумм SHA-256**: сервер (`handleChunkUpload`) теперь вычисляет SHA-256 на лету через `io.MultiWriter` во время приёма фрагментов. Финализация выгрузки (`handleCompleteUpload`) на роутере выполняется мгновенно (~0 мс) без повторного медленного чтения файла целиком с USB-диска, исключая таймауты сокетов.
  - **Поддержка параметра offset и дозаписи**: в API передачи фрагментов (`/upload/{session_id}/chunk?offset=...`) добавлена точная позиция смещения `f.Seek()`, позволяющая клиенту повторять отправку сбойных чанков.
  - **Автоматические повторы (Retries) в Android-клиенте**: в `ChunkedUploader` реализован цикл до 3 попыток отправки каждого чанка и финализации сессии с нарастающей паузой при сбоях Wi-Fi.
  - **Увеличение таймаутов HTTP**: таймауты чтения и записи в OkHttp (`ApiClient.kt`) и на сервере увеличены со 120s до 180–300s.
  - **Асинхронная индексация**: немедленный ответ клиенту об успешной выгрузке, фоновая генерация превью и добавление в индекс без блокировки сетевого соединения.
- **Защита от параллельного запуска нескольких синхронизаций (Race Conditions)**:
  - **Мьютекс на уровне репозитория (`SyncRepositoryImpl`)**: добавлен `Mutex.tryLock()` — любые повторные запросы синхронизации отсекаются с предупреждением в лог, пока идёт текущий процесс.
  - **Блокировка повторных нажатий в UI (`SettingsViewModel`, `GalleryViewModel`, `SettingsScreen`)**: кнопка «Синхронизировать сейчас» отключается во время активного процесса синхронизации, методы запуска проверяют активность корутины.
- **Таргетированное сканирование изменённых директорий (FSWatcher)**:
  - **Локальное сканирование папки при изменениях**: при возникновении событий создания, изменения или удаления файлов демон теперь сканирует **только ту директорию**, в которой произошли изменения (`ScanDirectory`), не обходя остальные папки библиотеки. Это кардинально снижает нагрузку на CPU роутера и ускоряет реакцию на добавление/удаление фото.
  - **Корректное удаление файлов из галереи**: при удалении файлов или каталогов удалённые медиафайлы вычищаются из индекса на лету, сохраняя неизменными все остальные папки.
  - **Полное периодическое сканирование сохранено**: фоновое плановое сканирование по таймеру (`scan_interval_minutes`), первичное сканирование при старте и ручное сканирование через API остаются **полными**, гарантируя 100% целостность библиотеки.

## [1.0.44] - 2026-09-10

### Folder Picker Fix & Native Mobile/TV App Integration
- **Исправление выбора папок в настройках**:
  - Устранена ошибка неработающей кнопки «Выбрать эту папку» (`confirmFolderPicker`): имя функции синхронизировано с обработчиком в интерфейсе, добавлена поддержка обратной совместимости, автоопределение текущего пути и генерация событий изменения формы (`input`, `change`).
  - Добавлена поддержка быстрого закрытия окна выбора директорий по клавише `Escape`.
- **Рекомендация нативных клиентов в PWA / Smart TV**:
  - Добавлено адаптивное модальное окно загрузки и установки нативных APK для Android TV (`SmartPhotoTV.apk`) и смартфонов (`SmartPhotoAndroid.apk`) при открытии PWA на соответствующих устройствах.
  - Оптимизирована навигация D-Pad (пульт ДУ) и добавлены опции «Продолжить в браузере» и «Больше не напоминать».

## [1.0.43] - 2026-09-10

### Full Video Support, FFmpeg Integration & Repeated Sync Loop Fix
- **Полная поддержка видеофайлов на сервере**:
  - В список поддерживаемых расширений (`supported_extensions`) добавлены видеоформаты: `.mp4`, `.mov`, `.mkv`, `.avi`, `.webm`, `.3gp`, `.m4v`.
  - Автоматическая миграция: существующие конфигурации `config.json` при обновлении автоматически пополняются видеоформатами.
  - Поддержка стриминга видео и перемотки по Range-запросам с корректными MIME-типами (`video/mp4`, `video/quicktime`, `video/webm`, `video/x-matroska` и др.).
- **Устранение бага циклической повторной выгрузки видео при синхронизации**:
  - В эндпоинт вычисления различий `/api/v1/sync/diff` добавлена проверка физического наличия файлов на диске: если файл существует на USB-накопителе с совпадающим размером, он помечается как `up_to_date` и исключается из повторной выгрузки, даже если фоновая индексация ещё не завершилась.
  - В процедуру завершения выгрузки `/api/v1/sync/upload/{id}/complete` добавлена немедленная регистрация нового файла в индексе галереи (`IndexSingleFile`), исключая создание ложных конфликтов `_conflict_...`.
- **Генерация миниатюр для видео и интеграция с FFmpeg**:
  - Встроенный Pure Go генератор постеров: при отсутствии внешних утилит сервер моментально создает качественные JPEG-превью с бейджем и значком Play (0% нагрузки на CPU и 0 RAM).
  - Поддержка FFmpeg: при наличии утилиты сервер извлекает точный первый кадр из видеопотока и сохраняет его в кэш миниатюр.
  - Модуль управления FFmpeg: проверка наличия в системе (`CheckFFmpeg`), отображение статуса в разделе «Самодиагностика» (Doctor) и в «Настройках».
  - Кнопка установки в 1 клик через Entware: возможность установить пакет `ffmpeg` прямо из веб-интерфейса роутера через `opkg`.
- **Встроенный видеоплеер в веб-интерфейсе**:
  - В Lightbox десктопного веб-интерфейса встроен нативный HTML5-плеер `<video controls>`, позволяющий просматривать синхронизированные видеозаписи прямо в браузере.
  - На карточках медиафайлов в таймлайне отображается бейдж видеозаписи (`▶ MP4`, `▶ MOV`).

## [1.0.42] - 2026-09-10

### Unified Telegram Community Channel
- **Обновление ссылок на сообщество Telegram**:
  - Все ссылки на официальный Telegram-канал переведены на единый канал проекта: [`t.me/SmartKeenetic`](https://t.me/SmartKeenetic).
  - Обновлены кнопки в шапке веб-интерфейса, блоке обратной связи, модальном окне обновлений и системном генераторе баг-репортов.

## [1.0.41] - 2026-09-04

### Centralized Bug Report System (Smart-Utils Style)
- **Система создания баг-репортов и обратной связи из Smart-Utils**:
  - Реализован системный модуль сбора телеметрии и безопасных журналов `internal/system/bugreport.go`.
  - Добавлены API-эндпоинты `GET /api/doctor/bugreport` и `POST /api/doctor/bugreport`.
  - В шапку веб-интерфейса добавлена кнопка `[ 🐞 Баг-репорт ]` (`.header-link-pill.bugreport`).
  - В раздел «Самодиагностика» (`#tab-doctor`) добавлены кнопки быстрого создания баг-репорта.
  - Интегрировано модальное окно `#modal-bug-report` с выбором компонентов экосистемы (`Smart-Photo`, `Smart-Utils`, `Smart-Route`, `Repo / Installer`), типов обращений (`bug`, `enhancement`, `question`), предпросмотром телеметрии и статусов служб, копированием в буфер обмена и открытием готового Issue на GitHub (`snakelair/Keenetic`).
  - Автоматическая маскировка паролей, токенов, ключей и хэшей при формировании отчёта.

## [1.0.40] - 2026-09-04

### Community Links & Discussion Integration
- **Интеграция ссылок на официальное сообщество и обсуждение**:
  - В шапку веб-панели добавлены быстрые аккуратные кнопки-пилюли: **Telegram** (`t.me/KeeneticSmartPhoto`) и **Форум Keenetic** (официальная ветка обсуждения).
  - В раздел «Клиенты и интеграция» добавлен интерактивный блок обратной связи для пользователей мобильного приложения Android и ТВ-клиента.
  - В раздел «Поблагодарить автора» добавлены прямые кнопки перехода в Telegram-канал и тему на форуме Keenetic.
  - В модальное окно истории версий и самообновления добавлены ссылки на обсуждение релизов.

## [1.0.39] - 2026-09-03

### Clean OPKG Installation & Unified API Status
- **Устранение дублирования информации при установке OPKG-скриптом**:
  - `package/entware/control/postinst` переведён на тихий фоновый запуск (detached graceful restart) без вывода устаревших баннеров с пустым IP.
  - Добавлен системный эндпоинт `GET /api/status`, возвращающий `status`, `version`, `router_model` и `web_port`.
  - Модель роутера (`router_model`) теперь корректно детектируется из `/tmp/sysinfo/model`, Device Tree и `ndmc`.
  - В установочный скрипт `install.sh` добавлена проверка занятости порта и диагностика таймаута старта.
  - Формат завершения установки полностью приведен в соответствие со **Smart-Utils**.

## [1.0.38] - 2026-09-02

### Self-Update System (Smart-Utils Style)
- **Интерактивная система самообновления в стиле Smart-Utils**:
  - Кликабельный бейдж версии в шапке (`#appVersionBadge`) для просмотра чейнджлога в любой момент.
  - Пульсирующий бейдж с индикатором новой версии (`.update-pulse-badge`, `.update-pulse-dot`) рядом с номером версии при наличии обновления.
  - Модальное окно обновления с баннером сравнения версий (Текущая ➔ Доступная), форматированным списком изменений и кнопкой мгновенного обновления.
  - Встроенная live-консоль хода установки и автоматический поллинг перезапуска службы с перезагрузкой страницы.
  - Исправлено закрытие модального окна выбора папок `folderPickerModal`.

## [1.0.37] - 2026-09-02

### OPKG Repository Release & Cache Busting
- **Официальный релиз OPKG в репозиторий Keenetic (`snakelair/Keenetic`)**:
  - Сборка пакетов для всех архитектур (`mipsel-3.4`, `armv7-3.2`, `aarch64-3.10`, `x86_64`, `mips-3.4`).
  - Добавление версионированных query-параметров к CSS и JS бандлам (`style.css?v=1.0.37`, `app.js?v=1.0.37`) для мгновенного сброса кэша браузера.
  - Гарантированное отображение разделов «Клиенты» и «Поблагодарить автора» без необходимости принудительной очистки кэша браузера.

## [1.0.36] - 2026-09-02

### Native Clients & Integration Hub (Раздел «Клиенты»)
- **Новый раздел «📱 Клиенты и интеграция»**:
  - Добавлена новая вкладка в верхнее меню веб-интерфейса.
  - Детальное описание архитектуры взаимодействия сервера на Keenetic (порты :8089 и :18089) с нативными клиентами.
  - Подробное описание возможностей мобильного клиента **SmartPhotoSync (Android)**: хронологический таймлайн в стиле Google Photos, статусы фото (LOCAL, SERVER, SYNCED, UPLOADING), фоновая служба WorkManager с 2MB-чанками и SHA-256 хешами, выбор папок устройства, Media3 плеер.
  - Подробное описание возможностей ТВ-клиента **SmartPhoto TV**: 100% оптимизация под пульт ДУ (D-Pad), кинематографическое слайд-шоу с эффектами Ken Burns (60 FPS), Cross-Dissolve, Slide, Zoom In, умный Shuffle Fisher-Yates, EXIF HUD оверлей.
  - Ссылки на прямую загрузку свежих сборок APK (`SmartPhotoAndroid.apk` и `SmartPhotoTV.apk`) из общего репозитория Keenetic, а также ссылки на запуск встроенной Web TV PWA.
  - Сводка по всем протоколам сопряжения (облачный 6-значный PIN через `ntfy.sh`, локальный ротируемый 4-значный PIN, постоянные токены).

## [1.0.35] - 2026-09-02

### Donate / Support Author Section (Поблагодарить автора)
- **Новый раздел «Поблагодарить автора»**:
  - Добавлена новая вкладка в навигацию веб-интерфейса.
  - Поддержка способов благодарности: ЮMoney (банковские карты РФ, кошелек, СБП, быстрый выбор сумм), Boosty (зарубежные и РФ карты, подписки и разовые донаты), Т-Банк (СБП, QR-код, перевод без комиссии).
  - Карточка экосистемы проектов Smart (Smart-Photo, Smart-Route, Smart-Utils) со ссылками на репозитории Keenetic и GitHub.
  - Быстрое копирование реквизитов и номеров кошельков в буфер обмена.

## [1.0.34] - 2026-08-30

### 6-Digit Cloud Pairing via ntfy.sh Relay (Zero IP Typing)
- **Облачное сопряжение по 6-значному PIN-коду**:
  - Генерация временного 6-значного PIN-кода (время жизни 5 минут) с автоматической публикацией адреса сервера и токена в публичный открытый брокер `ntfy.sh`.
  - Эндпоинты сервера: `POST /api/system/cloud-pair/create`, `GET /api/system/cloud-pair/status`.
  - Интеграция в веб-интерфейс Настроек: отображение 6 крупных ячеек PIN, таймер обратного отсчета (5 минут) и кнопка генерации.
  - Интеграция в PWA SmartPhoto TV: при вводе 6 цифр клиент автоматически запрашивает параметры с `ntfy.sh`, определяет адрес сервера и токен и входит без ручного ввода IP-адреса.

## [1.0.33] - 2026-08-30

### Multi-Token Support & 4-Digit Rotating Pairing PIN
- **Поддержка множества токенов доступа (Multi-Token)**:
  - Возможность создавать, именовать и отзывать индивидуальные токены доступа для каждого устройства (Smart TV, телефоны, ПК) в настройках сервера.
  - Сохранение обратной совместимости с основным `sync_token`.
  - Эндпоинты управления токенами: `GET /api/system/tokens`, `POST /api/system/tokens`, `DELETE /api/system/tokens`.
- **Быстрое сопряжение по 4-значному PIN-коду (Rotating PIN)**:
  - Автоматическая генерация и ротация 4-значного PIN-кода каждую минуту с обратным отсчетом таймера в настройках.
  - Эндпоинт сопряжения: `POST /api/v1/auth/pair` — при вводе 4 цифр на клиенте сервер генерирует и возвращает постоянный токен доступа.
  - Интеграция в PWA SmartPhoto TV: окно авторизации автоматически распознает 4-значный код и выполняет привязку в один клик.

## [1.0.32] - 2026-08-30

### SmartPhoto TV PWA Integration & Smart TV Auto-Detection
- **Интеграция PWA-клиента SmartPhoto TV**:
  - PWA-клиент из репозитория `SmartPhotoTV` (`dist/web-tv`) встроен в сервер и доступен по путям `/pwa/`, `/pwa`, `/tv`, `/tv/` как на основном веб-порту (`8089`), так и на порту Smart-Sync (`18089`).
  - Статические файлы PWA (HTML, CSS, JS, манифест, service-worker) доступны на порту синхронизации без требования токена авторизации.
  - Автоматическое определение Smart TV устройств (Tizen, WebOS, Android TV, Apple TV, Roku, FireTV и ТВ-браузеров) по заголовку User-Agent с автоматическим перенаправлением на полноэкранный 10-foot UI `/pwa/`.
  - Автоматическое определение адреса сервера `window.location.origin` без необходимости ручного ввода IP при запуске из встроенного PWA.
  - Встроенный механизм синхронизации PWA дистрибутива во все скрипты сборки (`scripts/build_packages.go`, `build.bat`, `build_all.bat`, `build.ps1`, `deploy_silent.ps1`).

## [1.0.31] - 2026-08-30

### Client-Selected Sync Direction Modes
- **Поддержка выбора направления синхронизации на стороне клиента**:
  - В запрос `POST /api/v1/sync/diff` добавлен параметр `direction`:
    - `"two_way"` (по умолчанию) — вычисление списков `to_upload` (на сервер) и `to_download` (на клиент);
    - `"upload_only"` (или `"backup"`, `"client_to_server"`) — вычисление только файлов для резервного копирования с клиента на роутер;
    - `"download_only"` (или `"restore"`, `"server_to_client"`) — вычисление только файлов для скачивания из фотоархива роутера на клиент.
  - Оптимизация нагрузки на CPU и оперативную память роутера при однонаправленных сценариях.

## [1.0.30] - 2026-08-30

### Android Client API on Smart-Sync Port
- **Расширение REST API на порту Smart-Sync (18089)**:
  - Выведен полный набор эндпоинтов галереи для создания полноценного мобильного клиента Android с единой авторизацией по токену (`X-Sync-Token`, `Authorization: Bearer`, либо URL query `?token=...` для библиотек Glide/Coil):
    - `GET /api/v1/photos` — лента фотографий с фильтрацией (годы, месяцы, дни, папки, камера, избранное) и пагинацией;
    - `GET /api/v1/photos/{id}` — детальные метаданные фото (EXIF, координаты, разрешение, размер);
    - `GET /api/v1/photos/{id}/thumb` — получение миниатюр (`size=small|large`);
    - `GET /api/v1/photos/{id}/file` (или `/original`) — потоковое воспроизведение оригиналов с поддержкой `HTTP Range`;
    - `POST /api/v1/photos/{id}/favorite` — переключение статуса «В избранном»;
    - `GET /api/v1/albums` — список альбомов с обложками и счетчиками фото;
    - `GET /api/v1/folders` — древовидная структура папок;
    - `GET /api/v1/timeline/summary` — сводка по датам для быстрого скроллбара;
    - `GET /api/v1/stats` — системная статистика индексатора.

## [1.0.29] - 2026-08-30

### Native Smart-Sync REST API & WebDAV Integration
- **Нативный легковесный Smart-Sync REST API (Вариант 1)**:
  - Выделенный порт по умолчанию: `18089` (настраивается в веб-интерфейсе).
  - Безопасная авторизация по секретному токену (`X-Sync-Token` / `Bearer`).
  - Потоковая чанковая загрузка файлов (Chunked Streaming) с буфером < 2 МБ RAM и проверкой хэшей SHA-256.
  - Политики разрешения конфликтов (`keep_both`, `server_wins`, `client_wins`, `backup_only`).
  - Эндпоинты: `/api/v1/sync/ping`, `/api/v1/sync/diff`, `/api/v1/sync/upload/init`, `/api/v1/sync/upload/{id}/chunk`, `/api/v1/sync/upload/{id}/complete`, `/api/v1/sync/download`, `/api/v1/sync/delete`.
- **WebDAV Сервер (Вариант 2)**:
  - Выделенный порт по умолчанию: `28089` (настраивается в веб-интерфейсе).
  - Поддержка подключения мобильных приложений автозагрузки фото (PhotoSync, FolderSync) и сетевых дисков Windows/macOS/Linux.
  - Basic Auth авторизация с поддержкой пароля.
- **Интеграция в веб-интерфейс и диагностику**:
  - Блок «Синхронизация и интеграция» в разделе «Настройки» с генерацией и копированием токенов.
  - Проверка статуса обоих сервисов в System Doctor.

## [1.0.28] - 2026-08-29

### Albums Dual View (Grid & Tree) & Custom Scrollbar Fix
- **Два режима отображения альбомов и папок**:
  - **Сетка карточек**: классический вид с обложками альбомов, количеством фото и размером на диске.
  - **Древовидный вид (Tree Explorer)**:
    - Интерактивное дерево каталогов с поиском по папкам в реальном времени.
    - Кнопки «Развернуть все» и «Свернуть все».
    - Быстрый просмотр фотографий внутри выбранной папки прямо в дереве или кнопка «Открыть в фотоленте».
    - Запоминание выбранного режима отображения в `localStorage`.
- **Исправление скролла**:
  - Восстановлен и стилизован глобальный плавный скроллбар во всех браузерах (Firefox, Chrome, Edge, Safari) с современным оформлением в стиле glassmorphism.

## [1.0.27] - 2026-08-28

### System Doctor Self-Diagnostics Revamp (Smart-Route Style)
- **Модернизированная самодиагностика системы**:
  - Карточка самодиагностики в стиле Smart-Route со сводными бейджами (`🟢 УСПЕШНО`, `🟡 ПРЕДУПРЕЖДЕНИЯ`, `🔴 ОШИБКИ`).
  - Категоризированный список проверок (Система, Ядро, RAM Heap, Точки монтирования USB, Каталоги фото, Права на запись в кэш, Индексатор, Web-сервер и безопасность).
  - Сворачиваемый блок системных выводов `Raw outputs` (`df -h`, `free -m`, `mount`, `uname -a`).
  - Кнопка быстрого копирования отчета в буфер обмена для поддержки.

## [1.0.26] - 2026-08-28

### Navigation Tabs Reordering
- **Раздел «Настройки» перенесен на последнюю позицию**:
  - Вкладка «Настройки» теперь замыкает меню навигации (`Фотолента` ➔ `Альбомы` ➔ `Избранное` ➔ `Фильтры & Поиск` ➔ `Индексация` ➔ `Журнал` ➔ `Диагностика` ➔ `Настройки`).

## [1.0.25] - 2026-08-28

### Real-Time Live Logs Terminal (Smart-Route Style)
- **Системный журнал в реальном времени**:
  - Живая трансляция логов через SSE без необходимости обновлять страницу.
  - Удобный фильтр уровней логов (`ALL`, `INFO`, `SUCCESS`, `WARN`, `ERROR`, `DEBUG`).
  - Мгновенный текстовый поиск по сообщениям и категориям с авто-фильтрацией при вводе.
  - Автопрокрутка к новым записям с возможностью фиксации скролла.
  - Точный таймстемп с миллисекундами (`HH:MM:SS.mmm`) и фирменная цветовая дифференциация.

## [1.0.24] - 2026-08-28

### Web UI Update Indicator & In-Place One-Click Updater
- **Индикатор обновления рядом с версией**:
  - При появлении новой версии в репозитории рядом с номером версии в шапке отображается анимированный золотистый бейдж `Обновление v1.0.X`.
- **Модальное окно со списком изменений (Changelog)**:
  - По клику на индикатор открывается модальное окно с подробным списком изменений от текущей версии до последней.
- **Бесшовное обновление в один клик**:
  - Кнопка «Обновить сейчас» автоматически инициирует фоновое обновление пакета через `opkg`, отслеживает перезапуск сервиса и обновляет веб-страницу.

## [1.0.23] - 2026-08-28

### Unified Header & Navigation Menu Design (Smart-Utils Style)
- **Обновленный дизайн шапки и меню в стиле Smart-Utils**:
  - Фирменный логотип с градиентным свечением `linear-gradient(135deg, #1f6feb, #38d39f)`.
  - Статусные индикаторы шапки: `Keenetic Router` бейдж, онлайн статус, живой индикатор SSE соединения `Live: Connected`.
  - Модернизированная панель навигации `.app-nav` с компактными кнопками, градиентной активной подсветкой и зелеными бейджами счетчиков.

## [1.0.22] - 2026-08-28

### URL Hash Routing & State Restoration on F5
- **Отображение раздела в URL**:
  - Текущая открытая вкладка теперь синхронизируется в URL хэше (`#photos`, `#albums`, `#favorites`, `#explore`, `#status`, `#settings`, `#logs`, `#doctor`).
  - При перезагрузке страницы по F5 (или переходе по прямой ссылке / истории браузера Назад-Вперед) открытый раздел мгновенно восстанавливается.

## [1.0.21] - 2026-08-28

### Web Interface Password Protection (Optional)
- **Пароль веб-интерфейса в настройках**:
  - В форму настроек добавлено поле «Пароль веб-интерфейса (опционально)».
  - При установке пароля галерея защищается HTTP Basic Auth (логин по умолчанию `admin`). При пустом значении авторизация отключается для свободного доступа.

## [1.0.20] - 2026-08-28

### Zero-Copy Progressive Full Photo Streaming in Lightbox
- **Устранение OOM-падений и перезагрузок при просмотре полноэкранных фото**:
  - **Zero-Copy Streaming**: в Lightbox полноразмерное фото теперь читается браузером напрямую через HTTP-стриминг файла (`/api/photos/{id}/file`), передавая декодирование аппаратному GPU смартфона/ПК. Роутер расходует **0 МБ Go RAM и 0% CPU**.
  - **Мгновенный Low-Res превью**: при перелистывании фото сначала отображается уже готовая маленькая миниатюра из кэша (0 мс), затем плавно обновляется на кристально четкий полноразмерный снимок.
  - **Оптимизация порядка поворота (`thumbnail.go`)**: масштабирование картинки теперь выполняется **ДО** поворота. Вместо поворота 48 млн пикселей (192 МБ RAM) поворачивается готовая крошечная миниатюра 320x240 (0.05 МБ RAM) — снижая расход памяти в 300 раз.

## [1.0.19] - 2026-08-28

### Embedded EXIF Thumbnail Fast-Path & Strict Viewport Lazy Loading
- **Мгновенное извлечение встроенных миниатюр EXIF (`ExtractEmbeddedThumbnail`)**:
  - При запросе сетки фото сервис читает готовые JPEG-миниатюры прямо из заголовков EXIF (IFD1). Для 90% фото с камер и смартфонов генерация занимает **0.001 секунды при 0% CPU и 0 МБ RAM**.
- **Строгий `IntersectionObserver` с `data-src` на клиенте**:
  - Браузер запрашивает миниатюры **исключительно для карточек, реально видимых на экране** (`rootMargin: '100px'`). Фоновые и непрокрученные карточки больше не создают лишней нагрузки на роутер.
- **Ограничение параллелизма (`max_concurrent_workers: 1`)**:
  - Строго последовательная генерация миниатюр предотвращает параллельные пики памяти и оставляет ядро CPU свободным для сети роутера.
- **Мгновенное освобождение памяти после масштабирования (`debug.FreeOSMemory`)**.

## [1.0.18] - 2026-08-28

### Strict <80MB Memory Budget & Zero-Allocation Timeline
- **Жесткий лимит памяти и устранение всплесков RAM**:
  - **`debug.SetMemoryLimit(80MB)` & `debug.SetGCPercent(20)`**: Go Runtime жестко удерживает использование памяти в рамках 80 МБ.
  - **Zero-Allocation Timeline**: прокрутка и пагинация фотоленты по умолчанию теперь читаются напрямую срезом из предсортированного массива, исключая создание промежуточных 80k-массивов при каждом HTTP-запросе.
  - **Кэширование статистики и альбомов**: `GetTimelineSummary`, `GetAlbums` и `GetStats` больше не обходят 80k фото при каждом тике SSE/запросе, а мгновенно возвращают предвычисленные структуры.
  - **Очистка `FullPath` в памяти**: устранено хранение избыточных абсолютных путей в оперативной памяти (пути генерируются на лету).

## [1.0.17] - 2026-08-28

### Pure Semantic Package Versioning (No Dash Suffix)
- **Удален суффикс релиза (`-1`) из пакетов OPKG**:
  - Имя пакетов, файл `control` и индекс `Packages` теперь используют строго чистую семантическую версию программы (`1.0.17` вместо `1.0.17-1`).
  - Полная синхронизация версий в менеджере пакетов OPKG (`smart-utils`), веб-интерфейсе и репозитории.

## [1.0.16] - 2026-08-28

### Deep RAM Footprint Optimization & String Interning
- **Глубокая оптимизация потребления оперативной памяти (RAM)**:
  - **Интернирование строк (`InternString` / `Compact`)**: устранено дублирование повторяющихся строковых метаданных (`Format`, `SourceRoot`, `Directory`, `Make`, `Model`, `Lens` и др.) в памяти. Для коллекций 80k+ фото экономится до 40 МБ RAM.
  - **Потоковый JSON декодер и энкодер**: чтение и сохранение `index.json` теперь используют потоковые `json.NewDecoder` и `json.NewEncoder` вместо единовременной аллокации 30+ МБ буферов в куче.
  - **Принудительный возврат памяти в ОС (`debug.FreeOSMemory`)**: память, задействованная во время сканирования и десериализации базы, немедленно возвращается ядру Linux.
  - **Тюнинг сборщика мусора Go (`debug.SetGCPercent(25)`)**: снижен порог удержания свободной памяти в куче для эффективной работы на роутерах с 128–512 МБ RAM.
  - **Снижение размера LRU-кэша по умолчанию до 4 МБ** (`mem_cache_size_mb: 4`).

## [1.0.15] - 2026-08-28

### Automated Repository & Documentation Version Synchronization
- **Автоматическая синхронизация версий в общем репозитории (`snakelair/Keenetic`)**:
  - Внедрена автоматическая синхронизация заголовков и бейджей во всех документах (`README.md`, `smart_photo_README.md`, `index.html`) при каждой сборке пакетов.
  - Версии в описании пакетов репозитория теперь строго соответствуют бинарной версии программы.

## [1.0.14] - 2026-08-28

### Instant Web Server Boot & Asynchronous Index Loading
- **Мгновенный старт веб-сервера (< 5 миллисекунд)**:
  - Порт `8089` теперь открывается немедленно при старте демона — полностью устранены задержки и ошибки 502 Bad Gateway при перезапуске.
  - Чтение и декодирование 30MB+ JSON-индекса (82k+ фото) перенесено в асинхронный фоновый поток.
  - Рекурсивный обход накопителя для `fsnotify` вотчера выполняется неблокирующе в фоне.
  - По готовности загрузки базы веб-клиенты мгновенно обновляются через SSE поток.

## [1.0.13] - 2026-08-28

### Fix Thumbnail & Photo File Resolution on Loaded Index
- **Динамическое восстановление абсолютных путей (`GetEffectiveFullPath`)**:
  - Устранена ошибка, из-за которой при загрузке базы из `index.json` поле `FullPath` оставалось пустым (`""`), вызывая отдачу заглушки «No Preview» для всех миниатюр.
  - Добавлено сохранение `full_path` в JSON и интеллектуальное восстановление физического пути по `SourceRoot` / `RelPath` / настроенным путям `photos_dirs`.
  - Превью и полноразмерные файлы теперь открываются моментально даже при переносе накопителя на другой роутер или смене точки монтирования USB.

## [1.0.12] - 2026-08-28

### Date Sanity Analysis & Automatic File Mtime Correction
- **Анализ и санитизация некорректных дат**:
  - Исключены даты из будущего (`> now + 24h`) и сброшенные в эпоху Unix (`1970-01-01` / нулевые даты).
  - Приоритет отдается валидным метаданным EXIF. Если дата в EXIF повреждена, используется дата файла или временной шаблон из имени файла (`IMG_YYYYMMDD_HHMMSS`).
  - **Автоматическая синхронизация даты файла на диске**: если в EXIF или имени файла содержится корректная дата, а дата модификации файла на диске (`mtime`) повреждена или сброшена, Smart-Photo автоматически обновляет `mtime` файла на диске через `os.Chtimes`.
  - Санитизация применяется как при первичном сканировании, так и при загрузке сохраненного индекса из кэша.

## [1.0.11] - 2026-08-28

### On-Demand Only Thumbnail Generation
- **Генерация миниатюр исключительно по запросу клиента (On-Demand)**:
  - Полностью отключена фоновая предварительная генерация миниатюр (`thumbQueue` и фоновые воркеры удалены).
  - Сканирование 80k+ фотографий теперь выполняется мгновенно и без паразитной нагрузки на CPU.
  - Миниатюры генерируются на лету при просмотре в браузере с семафорным ограничением параллелизма (`max_concurrent_workers`), защищающим RAM и CPU роутера от перегрузки при массовой подгрузке ленты.
  - Добавлено подробное логирование ошибок декодирования с указанием проблемного файла.

## [1.0.10] - 2026-08-28

### OPKG Package Integrity & Conffiles
- **Упаковка стандартного `config.json` в `data.tar.gz`**:
  - Полностью исключены предупреждения `file_sha256sum_alloc` при установке и обновлении пакета.
  - Синхронизированы контрольные суммы (MD5/SHA256) и размер пакета во всех архитектурных ветках.

## [1.0.9] - 2026-08-28

### Service Initialization & RAM Logging
- **Удален аргумент `-log /opt/var/log/smart-photo.log` из init-скрипта `S99smart-photo`**:
  - Теперь режим логирования полностью управляется конфигурационным файлом (`log_target: "memory"` по умолчанию) без принудительной перезаписи на диск.

## [1.0.8] - 2026-08-28

### OPKG Architecture & Repository Cleanliness
- **Полное устранение ошибки `has no valid architecture, ignoring` в OPKG**:
  - Каждая архитектурная ветка (`mipsel-3.4`, `aarch64-3.10`, `armv7-3.2`, `x86_64`, `mips-3.4` и их псевдонимы) теперь собирается со строгим соответствием архитектуры в пакете и в индексе `Packages`.
  - Удалены все устаревшие дубликаты пакетов из репозитория: для каждого сервиса (`smart-photo`, `smart-route`, `smart-utils`) хранится ровно один актуальный заголовок в каждом фиде.

## [1.0.7] - 2026-08-28

### Performance & CPU Optimization (0% Idle CPU)
- **Устранена паразитная нагрузка на процессор (100% CPU)**:
  - **Фильтрация событий файлового вотчера (`fsnotify`)**: Исключена циклическая переиндексация, вызывавшаяся записью служебных файлов кэша (`.smartphoto`), логов и временных файлов. Добавлена строгая фильтрация по поддерживаемым графическим форматам.
  - **Кэширование статистики миниатюр в памяти**: Устранен вызов `filepath.Walk` по всей директории кэша миниатюр каждые 3 секунды (добавлен TTL-кэш на 60 секунд).
  - **Облегченный SSE Heartbeat**: Заменен тяжелый 3-секундный пересчет статистики на легковесный ping-пакет, снижающий нагрузку процессора до 0% в режиме ожидания.
  - **Защита от записи пустого индекса**: Устранена перезапись `index.json` на накопитель при отсутствии фотографий.

## [1.0.6] - 2026-08-28

### Fixes & OPKG Architecture Compatibility
- **Исправление ошибки `has no valid architecture, ignoring` в OPKG**:
  - В поле `Architecture:` пакетов и индекса `Packages` установлено универсальное значение `all`, поддерживаемое абсолютно всеми устройствами и версиями Entware.
  - В репозиторий добавлены зеркальные псевдонимы директорий (например, `aarch64-k3.10` и `aarch64-3.10`, `mipsel-k3.4` и `mipsel-3.4`, `armv7sf-k3.2` и `armv7-3.2`).
  - В `install.sh` внедрено точное определение архитектуры непосредственно из `/opt/etc/opkg.conf`.

## [1.0.5] - 2026-08-28

### Realtime Watcher, Multi-folder & Live Discovery
- **Логирование в RAM по умолчанию**:
  - Режим логирования по умолчанию установлен в `"memory"` (кольцевой буфер в оперативной памяти), гарантируя нулевой износ Flash-памяти роутера.
- **Поддержка множества папок с фото (`photos_dirs`)**:
  - Добавлена возможность указывать произвольное количество папок и USB-накопителей одновременно (например, несколько подключенных флешек/дисков).
  - В настройках веб-панели реализован динамический список с кнопками добавления, удаления и встроенным проводником «Обзор...» для каждого пути.
- **Подписка на события файловой системы в реальном времени (`fsnotify`)**:
  - Индексатор автоматически отслеживает добавление, изменение, переименование и удаление файлов во всех настроенных каталогах через системные события файловой системы (с умным дебаунсом 1.5 сек).
- **Отображение результатов на лету без перезагрузки**:
  - При обнаружении и индексации новых фото они мгновенно транслируются через SSE-поток в браузер и аккуратно встраиваются в фотоленту с анимацией в реальном времени.

## [1.0.4] - 2026-08-28

### Fixes & HTTPS Installer Resilience
- **Исправление `wget: not an http or ftp url` в инсталляторе**:
  - Перед обновлением репозиториев `install.sh` автоматически устанавливает `wget-ssl`, `ca-certificates` и `ca-bundle` из стандартного HTTP репозитория Entware, наделяя менеджер пакетов `opkg` полноценной поддержкой HTTPS.
  - Добавлен автоматический fallback: если `opkg update` не может обновить HTTPS-фид, инсталлятор напрямую загружает `.ipk` пакет через проверенный `curl -sSL` и устанавливает его локально.

## [1.0.3] - 2026-08-28

### Repository Coexistence & Publishing Improvements
- **Единая витрина репозитория `snakelair/Keenetic`**:
  - Создана интерактивная главная страница `index.html` (веб-хаб) для демонстрации обоих проектов: **Smart-Photo** (порт 8089) и **Smart-Route** (порт 8088).
  - Подробные карточки функций, 1-клик команды быстрой установки по SSH и таблица поддерживаемых моделей Keenetic.
- **Надежная синхронизация при публикации**:
  - Скрипт `push_keenetic_repo.bat` синхронизирует и подтягивает актуальное состояние перед сборкой, гарантируя сохранность пакетов обоих сервисов в едином индексе `Packages` и `Packages.gz`.

## [1.0.2] - 2026-08-28

### Improvements & Mini File Manager
- **Скрытие стандартного браузерного скроллбара**:
  - Полностью убран громоздкий браузерный скроллбар во всем интерфейсе и фотоленте (`scrollbar-width: none` / `::-webkit-scrollbar { display: none }`).
  - Все типы прокрутки (колесо мыши, свайпы на сенсорных экранах, клавиатура, перетаскивание ползунка дат) работают идеально плавно.
- **Встроенный мини файловый менеджер для выбора папок**:
  - Добавлены кнопки «📁 Обзор...» в настройках для папки фотографий (USB) и отдельной папки кэша.
  - Модальное окно обзора файловой системы с быстрым переходом по точкам монтирования (`/tmp/mnt`, `/opt`, диски Windows `C:\`, `D:\`).
  - Навигация по директориям, переход «Вверх», выделение папки и подтверждение в один клик.

## [1.0.1] - 2026-08-28

### Improvements & Russian Localization
- **Полная русификация дат и времени**:
  - Все заголовки дней («Сегодня», «Вчера», «Пятница, 28 августа 2026 г.»), подписи («28 авг.»), названия месяцев («Август 2026») переведены на русский язык.
  - Отображение даты и времени в полноэкранном просмотрщике и панели EXIF в формате русской локали.
- **Индикатор версии приложения**:
  - В шапке интерфейса отображается версия приложения `v1.0.1` вместо статического текста.
  - Автоматическое отображение актуальной версии через API.
- **Интерактивный скроллер дат и годов в стиле Google Photos (справа)**:
  - Вертикальная шкала дат справа с метками годов (`2026`, `2025`...).
  - Перетаскиваемый ползунок (handle) с плавающим бейджем текущей даты при прокрутке.
  - Плавный переход к выбранному году / месяцу в один клик.

## [1.0.0] - 2026-08-28

### Initial Release: Personal Google Photos-like Server for Keenetic Entware
- **Google Photos Timeline Experience**:
  - Infinite scroll with chunked API loading (`IntersectionObserver`).
  - Sticky day headers with friendly date grouping ("Today", "Yesterday", "28 August 2026").
  - Responsive photo grid with lazy thumbnail loading and aspect ratio preservation.
- **Fullscreen Lightbox & Viewer**:
  - High-res image rendering with smooth zooming (mouse wheel / buttons), panning, and 90° rotation.
  - Interactive slideshow mode with auto-advancing timer.
  - Complete EXIF inspector: Camera Make & Model, Lens, Aperture (f-stop), Shutter Speed, ISO, Focal Length, and GPS OpenStreetMap link.
  - Keyboard navigation (Arrow keys, Esc, Space, I, +, -, 0) and mobile swipe gestures.
- **On-the-Fly Thumbnail Engine & Two-Tier Cache**:
  - Pure Go image decoders and high-speed bilinear resizers (`CGO_ENABLED=0`).
  - Configurable cache location: `.smartphoto/thumbs` in the photos folder root or custom path (`/opt/var/cache/smart-photo`).
  - RAM LRU cache for lightning-fast UI responsiveness.
- **Router Hardware Optimization**:
  - Configurable worker concurrency limits (default 2 workers) to preserve Keenetic router CPU performance.
  - Memory-only ring buffer logging (zero wear on router flash memory).
  - Background incremental indexing and directory watcher.
- **Multi-Architecture Entware OPKG Packaging**:
  - Full support for `mipsel-3.4`, `armv7-3.2`, `aarch64-3.10`, `x86_64`, `mips-3.4`.
  - Seamless coexistence in `snakelair/Keenetic` OPKG repository alongside `smart-route`.
  - 1-click deployment script `deploy_to_router.bat` via SSH.
