# 📘 Руководство пользователя SmartNVR

## 📑 Содержание
1. [Введение и архитектура](#1-введение-и-архитектура)
2. [Установка и запуск на роутере Keenetic](#2-установка-и-запуск-на-роутере-keenetic)
3. [Внешний доступ (KeenDNS / WAN / VPN)](#3-внешний-доступ-keendns--wan--vpn)
4. [Поиск и добавление камер и NVR](#4-поиск-и-добавление-камер-и-nvr)
5. [Просмотр видео в реальном времени (Live)](#5-просмотр-видео-в-реальном-времени-live)
6. [Работа с архивом записей (Playback)](#6-работа-с-архивом-записей-playback)
7. [PTZ-управление поворотными камерами iCSee](#7-ptz-управление-поворотными-камерами-icsee)
8. [Управление службой Entware и диагностика](#8-управление-службой-entware-и-диагностика)

---

## 1. Введение и архитектура

**SmartNVR** — высокопроизводительный шлюз видеонаблюдения и веб-центр для роутеров Keenetic (Entware) и ПК. Позволяет безопасно просматривать камеры и видеорегистраторы Hiseeu / Xiongmai / iCSee из любой точки мира через интернет без необходимости прямого проброса портов к камерам и без устаревших браузерных плагинов.

Веб-интерфейс работает на порту **`8095`** (`http://172.16.1.1:8095`).

---

## 2. Установка и запуск на роутере Keenetic

### Установка в 1 команду по SSH:
```bash
curl -sSL https://raw.githubusercontent.com/snakelair/Keenetic/main/install.sh | sh -s smart-nvr
```

### Развертывание с ПК:
В каталоге проекта SmartNVR запустите `deploy_to_router.bat` (или `deploy_to_router.ps1`).

---

## 3. Внешний доступ (KeenDNS / WAN / VPN)

Для доступа к камерам и архиву извне настройте в KeeneticOS:
* **KeenDNS:** переадресация `nvr.ваш-роутер.keenetic.link` на порт **`8095`**.
* **VPN:** подключитесь к домашнему WireGuard / Smart-VPN туннелю и откройте `http://172.16.1.1:8095`.

---

## 4. Поиск и добавление камер и NVR
1. Откройте вкладку **«Поиск устройств»**.
2. Нажмите **«Сканировать сеть»**.
3. Сканер проверит порты `34567` (NetSDK), `554` (RTSP) и `8899` (ONVIF).
4. Нажмите **«+ Добавить в SmartNVR»** на найденном устройстве.

---

## 5. Просмотр видео в реальном времени (Live)
* Сетки 1×1, 2×2, 3×3.
* Переключение потоков: SD (Субпоток — для мобильного интернета) и HD (Основной).
* Моментальный снимок кадра в формате JPEG.
* Полноэкранный режим.

---

## 6. Работа с архивом записей (Playback)
* Выбор канала NVR и даты в календаре.
* Наглядная 24-часовая временная шкала с зелеными блоками записей.
* Встроенный плеер и прямое скачивание видеофайлов через роутер.

---

## 7. PTZ-управление поворотными камерами iCSee
* Управление наклоном, поворотом и зумом для поворотных камер по протоколу NetSDK.

---

## 8. Управление службой Entware
```bash
/opt/etc/init.d/S99smart-nvr start|stop|restart|status
```
* Конфигурация: `/opt/etc/smart-nvr/config.json`
* Лог в RAM: `/tmp/smart-nvr.log`
